---@type Tween
local Tween = require(script:GetCustomProperty("Tween"))

---@type Class
local Class = require(script:GetCustomProperty("Middleclass"))

---@type Utils, Array
local Utils, Array = require(script:GetCustomProperty("Utils"))

---@type Factory_Events
local Factory_Events = require(script:GetCustomProperty("Factory_Events"))

return Class, Utils, Array, Tween, Factory_Events